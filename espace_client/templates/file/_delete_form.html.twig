<form method="post" action="{{ path('app_file_delete', {'id': file.id, 'user_login': user.login, 'category_label': category.label}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ file.id) }}">
    <button class="btn">Delete</button>
</form>
